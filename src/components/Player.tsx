import { PlayerBar, PlayerContainer, PlayerControl, PlayerInfo } from ".";

export const Player = () => {
  return (
    <PlayerContainer>
      <PlayerInfo />
      <PlayerBar />
      <PlayerControl />
    </PlayerContainer>
  );
};
